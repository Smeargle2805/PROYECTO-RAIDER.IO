import { StyleSheet } from "react-native";

export const logoPersonaje = StyleSheet.create({
    container: {
      shadowColor: '#000',
      shadowOffset: {
        width: 0,
        height: 10,
      },
      shadowOpacity: 0.3,
      shadowRadius: 4.65,
      elevation: 8,
      borderRadius: 10,
    },
    image: {
      width: 350,
      height: 200,
      borderRadius: 10,
    },
  });
